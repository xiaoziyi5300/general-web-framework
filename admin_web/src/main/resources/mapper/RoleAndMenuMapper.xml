<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cn.mapper.RoleAndMenuMapper">
    <resultMap id="BaseResultMap" type="com.cn.model.RoleAndMenu">
        <result column="id" property="id" jdbcType="INTEGER"/>
        <result column="role_id" property="roleId" jdbcType="INTEGER"/>
        <result column="menu_id" property="menuId" jdbcType="INTEGER"/>
    </resultMap>
    <insert id="insert" parameterType="com.cn.model.RoleAndMenu">
        insert into t_role_menu (id, role_id, menu_id
        )
        values (#{id,jdbcType=INTEGER}, #{roleId,jdbcType=INTEGER}, #{menuId,jdbcType=INTEGER}
        )
    </insert>
    <insert id="insertSelective" parameterType="com.cn.model.RoleAndMenu">
        insert into t_role_menu
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">
                id,
            </if>
            <if test="roleId != null">
                role_id,
            </if>
            <if test="menuId != null">
                menu_id,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">
                #{id,jdbcType=INTEGER},
            </if>
            <if test="roleId != null">
                #{roleId,jdbcType=INTEGER},
            </if>
            <if test="menuId != null">
                #{menuId,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <delete id="deleteByRoleId">
        delete t_role_menu where role_id = #{rId}
    </delete>

    <insert id="save">
        INSERT INTO `t_role_menu` ( id, roleId ,menu_id)
        VALUES
        <foreach collection="list" item="item" index="index" separator=",">
            ( #{id}, #{item})
        </foreach>
    </insert>
</mapper>